# 贡献指南

感谢帮助 DemoCraft 变得更可靠。当前为 Windows 预览版，优先欢迎可复现问题、文档修正、兼容性结果和带验证的聚焦修改。

## 提交问题前

阅读 [常见问题](docs/FAQ.md)，尝试用内置示例复现。请明确区分“已经发生的错误”和“希望增加的新能力”，并使用仓库提供的 Issue 模板。

不要在公开 Issue 中提交密码、访问令牌、客户视频或含有个人路径的完整工程。安全敏感问题不要附公开可利用的细节；若仓库提供私密安全报告入口，优先使用该入口。

## 本地开发

需要 Windows x64、Node.js 22+。导出和桌面集成测试另需 FFmpeg。依赖安装可能需要联网。

```powershell
npm ci
npm run check
npm test
npm start
```

如果 Electron 二进制缺失，请参阅 README 的安装脚本说明。桌面测试需要交互式 Windows 会话及测试素材，不能把无头环境中的失败直接认定为产品录屏故障。

## 修改原则

- 一个 PR 解决一个明确问题，避免顺带重写无关部分。
- 涉及剪辑、镜头或画面时，保持预览与导出共用渲染逻辑。
- 不绕过素材授权、sandbox、contextIsolation 或 IPC 检查来解决问题。
- 不引入未说明的素材上传、遥测或云服务依赖。
- 更改工程格式前说明兼容性与迁移策略。
- 不把 FFmpeg 等第三方组件直接按本项目 MIT 许可重新分发。

## 验证要求

| 修改类型 | 建议验证 |
| --- | --- |
| 文档 | 本地链接、命令和描述与实际功能一致 |
| 参数校验 / 时间计算 | 增加或更新核心单元测试 |
| 渲染效果 | 检查实际预览和导出短片，注明尺寸与时间点 |
| 工程 IO | 保存、重开、缺失素材与取消操作 |
| 录屏 / 音频 | 注明设备与权限，只录自建测试窗口 |
| 打包 | 启动打包后的程序，不只验证开发启动 |

`npm run check` 和 `npm test` 是基础检查，不代表所有桌面功能均通过。现有测试覆盖详见 [VERIFICATION.md](VERIFICATION.md)。

## PR 描述模板

```text
解决的问题：
修改范围：
验证命令及实际结果：
桌面验证环境（如适用）：
新增依赖或权限（如有）：
已知限制 / 尚未验证的情况：
```

如只是文档修正，请说明不涉及运行代码，避免让用户误以为发布了新功能或新安装包。
