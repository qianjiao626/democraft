# 验证记录 — v0.1.0 Preview

验证日期：2026-09-09（开发电脑本地日期）。环境：Windows 11 x64、Node.js 24.18.0、Electron 37.10.3、FFmpeg 9.0.1（本机独立安装）。

## 已实际执行

- 核心单元测试：8/8 通过（默认值隔离、工程版本、边界校验、缩放插值、短区间、输出尺寸、时间）。
- 主进程、桥接、编辑器和渲染器的 JavaScript 语法检查通过。
- 真正启动 Electron 桌面窗口，检查空状态和载入后界面截图。
- 导入 1280×720 测试 MP4，导出 3 秒 1920×1080 / 30 FPS；FFprobe 确认 H.264 视频、AAC 音频、时长 3 秒。
- 工程保存、覆盖保存、重开、标题撤销 / 重做通过。
- 取消导出通过：不留下目标 MP4。
- 真实窗口录制通过：仅录制测试脚本自己创建的虚构窗口，不录制用户桌面；WebM 保存、载入、跳到 1 秒通过。
- 竖屏与方形导出通过：1080×1920 / 1080×1080，裁剪 0.5–1.5 秒，每个输出 30 帧、时长 1 秒、保留 AAC 音频，包含聚焦与遮挡设置。此项检查链路与参数，不等同于逐像素遮挡覆盖证明。
- 自制 Orbit 虚构仪表盘录制约 6.5 秒，加入中文标题、字幕和聚焦，成功导出 `assets/showcase.mp4`（无音频来源）。
- 修复并复测：WebM 本地协议缺少 Range 支持导致 seek 失效；自动保存文件替换遇到 EXDEV；重复 change 事件污染撤销栈。

## 未作保证

- 未覆盖所有显卡 / 声卡、多显示器 DPI、系统音频与麦克风组合。
- 未做完整 5 分钟高分辨率压力测试、长时间内存监测或断电恢复。
- 未签名、未做第三方安全审计、未验证 macOS / Linux。
- 尚不具备自动鼠标聚焦、多区间镜头、多轨剪辑、自动字幕等功能。

自动化脚本位于 `scripts/`。集成测试应在交互式 Windows 桌面上运行，会短暂打开自己的测试窗口。CI 仅执行语法与单元测试，不把桌面集成测试结果冒充云端通过。

## Windows 分发包补充验证

已启动打包产物 `release/win-unpacked/DemoCraft.exe`，验证外置资源目录中的内置示例加载、FFmpeg 探测及 1 秒 MP4 导出成功。Windows ZIP 为未签名预览版，需完整解压；未在另一台干净电脑上验证。
